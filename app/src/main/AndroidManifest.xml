<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.limited.training.stamina">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stamina">
        <activity
            android:name=".activities.AgreementUser"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ProgressActivity"></activity>
        <activity android:name=".activities.PausedActivity"></activity>
        <activity android:name=".activities.RegisterActivity"
            android:windowSoftInputMode="stateVisible"
        ></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".activities.AgreementLocation"></activity>
        <activity android:name=".activities.MainScreen"></activity>
        <activity android:name=".activities.GoogleLogin"></activity>
    </application>

</manifest>